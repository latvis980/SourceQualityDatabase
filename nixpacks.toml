[phases.setup]
nixPkgs = [
  "python311",
  "chromium",
  "chromium-driver",
]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "playwright install chromium",
  "playwright install-deps chromium",
]

[start]
cmd = "python bot.py"
